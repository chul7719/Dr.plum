# 로컬 개발은 SQLite로 시작합니다. 운영 전환 시 PostgreSQL 등으로 바꾸세요.
# 예: DATABASE_URL="postgresql://user:password@localhost:5432/mart_maintenance"
DATABASE_URL="file:./dev.db"

# openssl rand -base64 32 명령으로 생성한 임의의 문자열을 넣으세요.
NEXTAUTH_SECRET="replace-with-a-random-string"
NEXTAUTH_URL="http://localhost:3000"
